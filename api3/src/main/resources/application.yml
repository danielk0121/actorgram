server:
  port: 40003

spring:
  application:
    name: api3

gemini:
  api-key: ${GEMINI_API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.actorgram.api3: DEBUG
